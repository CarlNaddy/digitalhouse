## 1. Setup, dependencies & foundations

- [x] 1.1 Add `Stripe.net` and `Ulid` (Cysharp) to `Directory.Packages.props` as `<PackageVersion>`; add `PackageReference`s (no `Version`) to `DigitalHouse.csproj`. Verify `dotnet restore` and `dotnet build DigitalHouse.slnx` are clean.
- [x] 1.2 Register `TimeProvider.System` as a singleton in `Program.cs` (nothing does today). Verify a test resolves `TimeProvider` and an existing test still passes. _(Option-a: verified by build + suite green; no tautology test added for the one-line registration.)_
- [x] 1.3 Add `ApplicationUser.StripeCustomerId` (`string?`). Verify an existing Identity test still passes.
- [x] 1.4 Add `Features/Marketplace/MarketplaceOptions.cs` (`ReservationWindow` 00:20:00, `BuybackSpreadCapCents` 1000, `CommissionBps` 0, `CatalogPageSize` 24, `PriceRecomputeCron` `*/10 * * * *`) + a `"Marketplace"` section in `appsettings.json`; bind via `AddOptions<MarketplaceOptions>().Bind(...).ValidateDataAnnotations()` in an `AddMarketplace()` extension called from `Program.cs`. Verify a test resolves `IOptions<MarketplaceOptions>` with the expected values. _(`MarketplaceOptionsTests`: binding + defaults + range validation.)_
- [x] 1.5 Add `Stripe:SecretKey` / `Stripe:PublishableKey` / `Stripe:WebhookSecret` to user-secrets (dev) and document the env-var form for prod. Verify a test using `appsettings.Testing.json` / the fixture resolves a Stripe config value. _(Placeholder values set in user-secrets — replace with real Stripe test keys. Env form (`Stripe__SecretKey`, …) documented on `Features/Payments/StripeOptions.cs`. `StripeOptionsTests` binds from in-memory config; `StripeOptions` is not `ValidateOnStart` so seed/CI boot without keys.)_
- [x] 1.6 `AppDbContext.SaveChangesAsync`/`SaveChanges` override: for each `Modified` `Product`, throw `InvalidOperationException` if `CurrentPriceMicros` is modified unless `AllowPriceWrites` is set (an `AsyncLocal<bool>` toggled by a `using db.AllowPriceWrites()` disposable), and always throw if `PriceSeed`, `CreatedAt`, or `PublicId` is modified. `Added` entities never trip it. Verify xUnit `DatabaseTest`s: mutating `CurrentPriceMicros` outside the disposable throws; inside it saves; mutating `PriceSeed`/`CreatedAt`/`PublicId` always throws; the stored values are unchanged after a rejected save. _(Instance `bool` flag, not `AsyncLocal` — correct scoping: engine's `using` + save share one context. `ProductPersistenceTests`: 5 guard cases + a positive `AllowPriceWrites` case, all green against real Postgres.)_

## 2. Data model & migration

- [x] 2.1 `Data/Product.cs` + `Data/Configurations/ProductConfiguration.cs` + `tests/DigitalHouse.Tests/TestData/ProductBuilder.cs`: `Slug`, `Title`, `Description`, `PublicId` (`Ulid`, see §4), `PriceSeed` (`long`), `CreatedAt` (`DateTimeOffset`), `CurrentPriceMicros` (`long`), `BuybackSpreadCapCents` (`long?`), `UpdatedAt` (`DateTimeOffset`). No `BasePriceMicros`/`AnnualFactor`/`NoiseAmplitude`/`PriceFloorMicros`/`PriceCeilingMicros`/`ExistsSince`. `PublicId`, `PriceSeed`, and `CreatedAt` have private setters. `ProductBuilder` (Bogus, fixed seed) sets `PublicId = Ulid.NewUlid()`, a `PriceSeed`, a `CreatedAt` default of `now - 2 months` (overridable), and `CurrentPriceMicros = 1_000_000` unless pinned. Verify a builder test asserts `PriceSeed != 0`, a distinct 26-char `PublicId`, and micro values are `long`. _(Used `init` accessors, not `private set`, for the immutables — settable in the object initializer the builder/seeder/admin use, immutable after. `ProductBuilderTests` (6) + `ProductPersistenceTests` round-trip green.)_
- [x] 2.2 `Data/ProductImage.cs` + config + builder: `ProductId`, `StoredFileId` (FK to existing `StoredFile`), `Position` (`int`), `IsPrimary` (`bool`). Navigation `Product.Images` ordered by `Position`. Verify a `DatabaseTest` creates a gallery of 3 and reads them ordered with exactly one primary. _(`ProductImageTests`. Ordering is a query concern — EF has no ordered nav; loaders `.OrderBy(i => i.Position)`.)_
- [x] 2.3 `Data/AssetOwnership.cs` + config + builder: `UserId` (`string?`, null = marketplace), `AcquiredAt`, `ReleasedAt` (`DateTimeOffset?`), `BuyPriceCents` (`long`), `AcquisitionType` enum, `SourcePurchaseId` (`long?`). Filtered unique index `HasIndex(x => x.ProductId).IsUnique().HasFilter("\"ReleasedAt\" IS NULL")`; index `(UserId, ProductId)`. Verify a `DatabaseTest` asserts a second current-ownership row for one product throws `DbUpdateException`. _(`AssetOwnershipTests` — 2 cases incl. released rows unconstrained. `UserId` is a plain string, not an `AspNetUsers` FK — the `null` = marketplace case rules a FK out.)_
- [x] 2.4 `Data/Reservation.cs` + config + builder: `UserId`, `QuotedPriceMicros`/`QuotedPriceCents` (`long`), `ResaleListingId` (`long?`), `Status` enum (`Active=0`/`Consumed`/`Expired`/`Cancelled`), `ExpiresAt`, `ConsumedAt` (`DateTimeOffset?`). Filtered unique index `HasFilter("\"Status\" = 0")`. Verify a `DatabaseTest` asserts two active reservations for one product cannot coexist. _(`ReservationTests`. Enums stored as **string** (matches `Listing.Status`); filter is `"\"Status\" = 'Active'"`, not `= 0`.)_
- [x] 2.5 `Data/ResaleListing.cs` + config + builder: `SellerId`, `Status` enum (`Active=0`/`Sold`/`Cancelled`), `ListedAt`, `ClosedAt`, `SoldPurchaseId` (`long?`). Filtered unique index `HasFilter("\"Status\" = 0")`. Verify a `DatabaseTest` asserts two active listings for one product cannot coexist. _(`ResaleListingTests`. Filter `"\"Status\" = 'Active'"`.)_
- [x] 2.6 `Data/PricePoint.cs` + config (append-only, no `UpdatedAt`): `PriceMicros` (`long`), `CapturedAt`, `Cause` enum (`Scheduled` — only value). Verify a `DatabaseTest` writes points and reads them ordered by `CapturedAt`. _(`PricePointTests` — 2 cases incl. `Cause` persisted as its string name.)_
- [x] 2.7 `Data/Purchase.cs` + config + builder: `ReservationId`, `BuyerId`, `SellerId` (`string?`), `PriceCents` (`long`), `StripePaymentIntentId` (`string?`), `CommissionCents` (`long`), `Status` enum (`Pending`/`Paid`/`Completed`/`Failed`/`Reversed`), `IdempotencyKey` unique index. Verify a `DatabaseTest` asserts a duplicate `IdempotencyKey` is rejected. _(`PurchaseTests`. Product FK is `Restrict`.)_
- [x] 2.8 `Data/Wallet.cs` (`UserId` unique, `BalanceCents` `long`, `Cashable` `bool` default true) + `Data/WalletTransaction.cs` (signed `AmountCents` `long`, `Type` enum `SaleCredit`/`BuybackCredit`/`Commission`/`RefundClawback`/`Adjustment` — with `PayoutDebit`/`PayoutReversal` defined but never written, `ReferenceKind` enum + `ReferenceId` `long?`, `BalanceAfterCents` `long`, `Meta` `jsonb`, append-only) + configs + builders. Verify a `DatabaseTest` sums transactions and matches `Wallet.BalanceCents`. _(`WalletTests` — 3 cases: sum==balance, jsonb `Meta` round-trip (STJ `ValueConverter`+`ValueComparer`), duplicate-user rejected.)_
- [x] 2.9 `Data/StripePayment.cs` + config: `PaymentIntentId` unique, `PurchaseId` (`long?`), `AmountCents` (`long`), `Status`, `ProcessedAt`. Verify a `DatabaseTest` asserts a duplicate `PaymentIntentId` is rejected. _(`StripePaymentTests`. `Status` is a free `string` — Stripe intent statuses are open-ended.)_
- [x] 2.10 `dotnet ef migrations add AddMarketplace`; wire a `MarketplaceSeeder` into `Data/Seed/` — 30 products with image galleries, random `PriceSeed`, and `CreatedAt` spread uniformly over `[2015-01-01, now]`; a ~1-in-3 pre-owned block; wallets for the seeded users; call `PricingEngine.RecomputeAsync` per product so `CurrentPriceMicros` + one snapshot reflect the backdated age. Verify `dotnet run -- seed` populates the catalog with prices well above $1 for old products, and `dotnet ef database update` on a fresh DB is clean. _(Migration `20260909182448_AddMarketplace` applied clean. `MarketplaceSeeder` wired into `SeedCommand`; `dotnet run -- seed` → 30 products ($0.97–$32.83, spread by age; a young one dips <$1 by design), one **deterministic SVG placeholder** per product via `IFileStore` (real image galleries land with the admin gallery work / catalog UI), 30 `Scheduled` snapshots, 10 pre-owned + 5 listed by the dev-admin demo owner, 1 wallet. `db.ChangeTracker.Clear()` after the recompute loop so the seeder's stale-tracked products don't trip the price gate.)_

## 3. Price curve & engine

- [x] 3.1 `Features/Marketplace/PriceCurve.cs`: `const` fields (`K=5`, `SecondsPerYear`, `BaseMicros=1_000_000`, drift/vol ranges, `BasePeriodYears`, `PeriodRatio`, `AmpBase`, `AmpRatio`, jitter range). `FromSeed(long): PriceCurve` derives `_drift`, `_vol`, and `K` components `{Period, Amp, Phase}` from `SHA256.HashData` of `"{seed}:{field}:{k}"`; the constructor precomputes `_oscAtZero`. `PriceMicrosAt(double ageSeconds): long` = `round(BaseMicros * exp(drift*y + vol*(Osc(y)-oscAtZero)))` with `y = Math.Max(0, ageSeconds/SecondsPerYear)`, clamped to `long.MaxValue`. (Pure — no DB, no clock.) _(Per-seed `ConcurrentDictionary` cache since curves are immutable and `FromSeed` is hot. Hash input built with `string.Create(InvariantCulture, …)` for CA1305.)_
- [x] 3.2 `PriceCurveTests` (pure unit tests): anchor (`PriceMicrosAt(0) == 1_000_000` for 50 random seeds); negative age → `1_000_000`; determinism (same seed+age across calls and across two `FromSeed`); seed variety (two seeds → > 5% mean divergence over year-5 samples); non-linearity (log-price second differences over 4 years are not all within a tight band); long-run drift (mean price over year-10 samples > mean over year-1 samples for 30 seeds); no overflow / always > 0 across 0..25 years. _(7 tests, all green.)_
- [x] 3.3 `Features/Marketplace/PricingEngine.cs` — `PriceAt(Product p, DateTimeOffset t): long` → `PriceCurve.FromSeed(p.PriceSeed).PriceMicrosAt((t - p.CreatedAt).TotalSeconds)`. Verify a test: `PriceAt(p, p.CreatedAt)` is `1_000_000`; `PriceAt` for a 5-years-old product is well above `1_000_000`. _(`PriceAt` is `static` — pure, no DI needed to call it.)_
- [x] 3.4 `PricingEngine.RecomputeAsync(Product, CancellationToken)`: transaction + `SELECT … FOR UPDATE` the product row, set `CurrentPriceMicros = PriceAt(product, timeProvider.GetUtcNow())` inside `using db.AllowPriceWrites()`, append a `PricePoint` `Cause=Scheduled`. Verify a `DatabaseTest` asserts the cache and one snapshot are written and match `PriceAt(now)`. _(Lock via `db.Products.FromSql($"… FOR UPDATE")` — loads the tracked, row-locked entity in one step. Also syncs the passed-in instance's `CurrentPriceMicros`/`UpdatedAt`. `PricingEngineTests`: recompute + twice-stable, 2 cases.)_
- [x] 3.5 `PricingEngine.GrowthLast12Months(Product): long` (cents) = `round((PriceAt(now) - PriceAt(max(now.AddYears(-1), CreatedAt))) / 10_000)`. Verify xUnit tests (fixed `FakeTimeProvider`) for a >1yr product and a <1yr product. _(`Microsoft.Extensions.TimeProvider.Testing` added to the test project.)_
- [x] 3.6 Architecture test scanning `Features/`, `Components/`, `Endpoints/`, `Data/`: assert no file outside the allowlist (`Data/Product.cs`, `Features/Marketplace/PricingEngine.cs`, `Data/Seed/MarketplaceSeeder.cs`, `Features/Marketplace/ProductAdminService.cs`, `tests/DigitalHouse.Tests/TestData/ProductBuilder.cs`) assigns `Product.CurrentPriceMicros` / `.PriceSeed` / `.CreatedAt` / `.PublicId`. Verify it passes and would fail on a planted violation. _(`ProductImmutabilityArchitectureTests` — greps for `CurrentPriceMicros`/`PriceSeed`/`PublicId` assignments (Product-unique tokens); `CreatedAt` is shared with other entities so it's covered by `init`-only + the runtime gate instead of the grep. `Data/Migrations/` excluded (EF-generated).)_

## 4. Certificate ID (`PublicId`)

- [x] 4.1 `Data/Product.cs` + `ProductConfiguration`: `PublicId` (`Ulid`, private setter) mapped `character(26)` fixed-length + unique via `ValueConverter<Ulid,string>(v => v.ToString(), s => Ulid.Parse(s))`; `NOT NULL` in the `AddMarketplace` migration (greenfield — no backfill). `ProductBuilder`, `MarketplaceSeeder`, and `ProductAdminService.CreateAsync` each set `PublicId = Ulid.NewUlid()` on the new entity. Verify a `DatabaseTest`: two persisted products have distinct 26-char `PublicId`s; the column is `NOT NULL` + unique. _(Done as part of §2.1 — `init` accessor not `private set`. `ProductPersistenceTests` round-trips it; `ProductBuilderTests` asserts distinctness; migration column is `character(26)` `NOT NULL` unique. `ProductAdminService` is `add-product-administration` — not in this change.)_
- [x] 4.2 `AppDbContext` guard (task 1.6) also rejects a `Modified` `Product.PublicId`. Verify a `DatabaseTest`: mutating `PublicId` on a tracked persisted product and saving throws `InvalidOperationException` and the stored value is unchanged; a fresh `Added` product with a `PublicId` saves fine. _(Done as part of §1.6 — `ProductPersistenceTests.Changing_the_certificate_id_on_a_persisted_product_is_rejected`.)_
- [x] 4.3 `Features/Marketplace/CertificateId.cs` — `Masked(Ulid): string` (first 4 chars + `new string('•', 18)` + last 4 chars) and `For(Product, ApplicationUser? viewer, AssetOwnership? currentOwnership, bool isAdmin): string` (full when `isAdmin` or `viewer` is the current owner; `Masked(...)` otherwise). Verify xUnit tests: mask shape (real first/last 4, length 26, no other id chars); `For` returns full for current owner, full for admin non-owner, masked for other user / guest / former owner. _(`CertificateIdTests` — 6 cases.)_
- [x] 4.4 `ProductDetail.razor` renders a mono "Certificate ID" row via `CertificateId.For(...)` (product + current ownership + `await AuthState.IsInRoleAsync("Admin")`, computed in `OnInitializedAsync`), plus a muted "Registered to you" line when the viewer is the current owner. `MyAssets.razor` shows `row.Product.PublicId.ToString()` (raw, full). Admin `ProductIndex.razor` / `ProductEdit.razor` show the raw value. `Catalog.razor` shows nothing. Verify bUnit tests: owner sees full + "Registered to you"; non-owner sees masked and not that line; catalog cards show neither form. _(Done for `ProductDetail` (`OnParametersSetAsync`) — `ProductDetailPageTests`: anon → masked + no "Registered to you"; owner → full + "Registered to you". `CatalogPageTests` already asserts no id on cards. `MyAssets.razor` cert column lands with §13.2; the admin screens belong to `add-product-administration`.)_
- [ ] 4.5 Lifecycle stability: an xUnit `DatabaseTest` creates a product, captures `PublicId`, runs it through buy → peer resell → marketplace buyback → (once `add-product-administration` lands) retire/unretire → title+slug edit, asserting `PublicId` is unchanged at every step (fresh context between steps). _(Deferred — needs the purchase/resale/buyback services from §7–8.)_

## 5. Payment gateway seam

- [x] 5.1 `Features/Payments/IPaymentGateway.cs` (`CreatePurchaseIntentAsync`, `VerifySucceededAsync`, `RefundAsync`) + `IntentDto`. Verify it type-checks and is registered in `AddMarketplace()`. _(`IntentDto` → `record PaymentIntentDto(PaymentIntentId, ClientSecret)`. `IPaymentGateway` + `StripeWebhookProcessor` registered in `AddMarketplace()`.)_
- [x] 5.2 `Features/Payments/StripePaymentGateway.cs` using `Stripe.net` (`PaymentIntentService`, `RefundService`), passing a Stripe idempotency key via `RequestOptions`. Verify a test with `Stripe.net`'s HTTP client stubbed asserts the correct params are sent. _(Injects `IStripeClient` (registered from `StripeOptions.SecretKey`). Creates a Stripe Customer lazily → persists `ApplicationUser.StripeCustomerId`. `StripePaymentGatewayTests`: a capturing `IHttpClient` asserts `amount`/`currency=usd`/`metadata[userId]` on the PI request + `VerifySucceededAsync` truth table.)_
- [x] 5.3 `Features/Payments/FakePaymentGateway.cs` for tests (configurable succeed/fail, records refunds), registered in the Testing environment / test fixture. Verify a test toggles success and failure. _(Lives in `tests/…/Fakes/` — there is no host-based test env to swap it into (Option-a); §7–8 service tests `new` it directly. `FakePaymentGatewayTests`: toggle + `Intents`/`Refunds` recording.)_
- [x] 5.4 `Endpoints/StripeWebhookEndpoints.cs` — `POST /api/stripe/webhook`, `.DisableAntiforgery()`, unauthenticated: verify the signature via `EventUtility.ConstructEvent` + `Stripe:WebhookSecret`, handle `payment_intent.succeeded` / `checkout.session.completed`, dedupe on Stripe event id AND `StripePayments.PaymentIntentId`, advance the related `Purchase`. Verify xUnit tests: valid event, invalid signature (4xx, no state change), duplicate delivery (single advance). _(Verification/state logic extracted to a testable `Features/Payments/StripeWebhookProcessor`; the endpoint is a thin body-read + `Ok/BadRequest` wrapper. Idempotency on a new `ProcessedWebhookEvent` table (migration `20260909185711_AddProcessedWebhookEvent`) + `StripePayments.PaymentIntentId` upsert + `Pending→Paid` guarded transition. `throwOnApiVersionMismatch: false`. `StripeWebhookProcessorTests`: signed-valid, bad-signature-no-change, redelivered-once.)_

## 6. Wallet & ledger

- [x] 6.1 `Features/Marketplace/WalletService.cs` — `PostAsync(Wallet, long signedCents, WalletTransactionType type, WalletReference reference, IReadOnlyDictionary<string,string>? meta, CancellationToken)`: transaction with `SELECT … FOR UPDATE` on the wallet row; append an immutable transaction + update `BalanceCents` + set `BalanceAfterCents` + recompute `Cashable = BalanceCents >= 0`. Positive for `SaleCredit`/`BuybackCredit`; negative only for `RefundClawback`/`Adjustment`. Verify `DatabaseTest`s: credit raises balance, clawback may go negative and sets `Cashable = false`, `balance == sum(transactions)` always. _(Signature: `PostAsync(AppDbContext db, long walletId, …)` — it operates on the **caller's** context + transaction (so the §8/§9 transfers stay atomic with the ledger write) and does not `SaveChanges`; row-locks via `FromSql(… FOR UPDATE)`. `WalletReference` readonly-struct + `WalletReferenceKind`/`ReferenceId`. Sign rule: `SaleCredit`/`BuybackCredit` > 0; `Commission`/`RefundClawback` < 0 (task 6.1 omitted `Commission` — it's a withholding, so negative; commission is a **separate entry** per the spec, only written when `> 0`); `Adjustment` ≠ 0; `PayoutDebit`/`PayoutReversal` → `NotSupportedException` (reserved). `WalletServiceTests`: 8 cases incl. the sum invariant and every sign guard.)_
- [x] 6.2 `WalletService.ForUserAsync(string userId)` get-or-create helper. Verify a `DatabaseTest` creates one wallet on first call and returns the same on the second. _(Race-safe: on a `DbUpdateException` from the `UserId` unique index it clears the tracker and re-reads.)_
- [x] 6.3 Wire wallet creation lazily on first proceeds (or at registration). Verify a `DatabaseTest` finds a zero-balance wallet. Confirm there is no top-up and no spend path. _(`ForUserAsync` is the lazy-create; the §8/§9 flows call it before crediting. `IPaymentGateway` never reads a wallet; `WalletService` exposes only `PostAsync` (credit / clawback / adjustment types) — no spend, no top-up. `WalletServiceTests.ForUserAsync_creates_one_zero_balance_wallet…` covers the zero-balance assertion.)_

## 7. Ownership & eligibility

- [x] 7.1 `Features/Marketplace/OwnershipService.cs` — `CurrentHolderAsync(Product)` and `IsCurrentOwnerAsync(string userId, Product)` querying `AssetOwnerships` on `ReleasedAt == null`. Verify `DatabaseTest`s for marketplace-held, user-held, former-owner cases. _(A never-traded product has **no** ownership row (`CurrentHolderAsync` → null); a post-buyback product has a `UserId == null` row — `IsMarketplaceHeldAsync` treats both as marketplace-held. Static `CurrentHolderAsync(AppDbContext, productId, ct)` overload for in-transaction callers. `OwnershipServiceTests`: 4 cases.)_
- [x] 7.2 `Features/Marketplace/PurchaseEligibility.cs` — `CheckAsync(ApplicationUser buyer, Product)`: deny if buyer is the current owner; deny if not buyable (owned AND no active resale listing); deny if an active reservation is held by someone else (after lazy-expiring stale ones); otherwise allow. Verify xUnit tests for each denial and the allow case, including "former owner may buy a listed product". _(Returns `Eligibility` (`IsAllowed` + `EligibilityReason`). Lazy-expire extracted to `ReservationExpiry.ExpireStaleAsync` (shared with §7.3, §8, §10). `PurchaseEligibilityTests`: 7 cases incl. own-reservation-doesn't-block and stale-reservation-expired-inline.)_
- [x] 7.3 `Features/Marketplace/BuybackEligibility.cs` — `Check(ApplicationUser owner, Product, long currentPriceCents, long buyPriceCents)`: allow when `currentPriceCents - buyPriceCents <= spreadCap` OR `currentPriceCents <= buyPriceCents`; deny if not current owner, or an active listing/reservation exists. Verify xUnit tests: within cap, above cap (denied), at a loss (allowed), non-owner (denied), listed/reserved (denied). _(Split: pure `static WithinSpreadCap(current, buy, cap)` for the math + `CheckAsync(owner, product, currentPriceCents, ct)` which derives `buyPriceCents` from the current ownership row and `spreadCap` from `product.BuybackSpreadCapCents ?? options`. `BuybackEligibilityTests`: 4 `[Theory]` math rows + 6 stateful cases.)_

## 8. Reservation & purchase flow

- [x] 8.1 `ReservationService.ReserveAsync(ApplicationUser, Product, ResaleListing?, CancellationToken)`: lazy-expire any stale active reservation; run `PurchaseEligibility`; insert a `Reservations` row `Active` with `ExpiresAt = now + ReservationWindow`, quoted price from `CurrentPriceMicros`. Verify `DatabaseTest`s: a successful reserve locks the quoted price; a concurrent second reserve hits the filtered unique index and is mapped to "reserved, try again later"; the owner cannot reserve their own product. _(Quotes from the **freshest** `CurrentPriceMicros` (re-read), not the caller's instance. Unique-violation (`23505`) → `MarketplaceException(Reserved)`. `ReservationAndPurchaseTests`: price-lock, owner-denied, concurrent-reject (1 win + 1 `Reserved` + exactly one active row).)_
- [x] 8.2 `PurchaseService.QuoteAsync(Reservation)`: create a `Purchases` row `Pending` with `PriceCents = QuotedPriceCents` and `IdempotencyKey = $"purchase:{reservationId}"`. Verify a `DatabaseTest` creates the pending purchase at the quoted price. _(Idempotent — existing key returns the existing purchase; `23505` race → clear tracker + re-read. Resolves `SellerId` from the resale listing (or null = marketplace).)_
- [x] 8.3 `PurchaseService.CompleteAsync(Reservation, PaymentIntentResult, CancellationToken)` pre-transfer step: reject if the reservation is not `Active` or is past `ExpiresAt`; confirm the Stripe PaymentIntent for the full `PriceCents` reached `succeeded` via `IPaymentGateway`; on failure set the purchase `Failed` and return an error, leaving the reservation for retry. Verify tests with `FakePaymentGateway` for success and failure. _(`PaymentConfirmation(PaymentIntentId)` record. Status trail: `Pending → Paid → Completed`/`Reversed`, or `Pending → Failed`. Status writes use `ExecuteUpdate` on fresh contexts so they survive a rolled-back transfer.)_
- [x] 8.4 `PurchaseService.CompleteAsync` DB transaction: `SELECT … FOR UPDATE` on the product; re-check the reservation still `Active`/unexpired and matching product; re-check the resale listing still `Active` for peer buys; close the seller ownership row, insert the buyer row (`BuyPriceCents = QuotedPriceCents`), credit the seller wallet `QuotedPriceCents - commission` via `SaleCredit` (only when the seller is a user), record the `Commission` entry, close the listing `Sold` with `SoldPurchaseId`, set the reservation `Consumed`, the purchase `Completed`. No buyer wallet movement. Verify `DatabaseTest`s: marketplace purchase, peer purchase with commission, expired-reservation rejection. _(Locks + loads the product via `FromSql(… FOR UPDATE)`. Two ledger entries on a peer sale — `SaleCredit(+Q)` and `Commission(-c)` (only when `c > 0`), net `Q − c`; `WalletService.PostAsync` runs on the transfer's context so it's atomic. Seller wallet is ensured (via `ForUserAsync`) **before** `BeginTransaction`. `ReservationAndPurchaseTests`: marketplace (no wallet movement), peer @ 2.5% commission (entry order + amounts + listing `Sold` + `PeerPurchase`), expired-reservation.)_
- [x] 8.5 Transfer-transaction failure handling: if the task-8.4 transaction throws, roll back, refund the Stripe charge via the gateway, and set the purchase `Reversed`. Verify a test forces an exception inside that transaction and asserts refund + no ownership change + no ledger entry. _(`CompleteAsync` wraps `TransferAsync` in `catch (Exception)` — **any** post-payment failure (incl. a `MarketplaceException` from an in-tx re-check) → `RefundAsync` (logged, not rethrown, if the refund itself fails — `MarketplaceReconcileJob` sweeps) + purchase `Reversed`, then rethrow. Test: seller delists after payment → re-check fails → `Refunds == [("pi_ok", Q)]`, purchase `Reversed`, no buyer ownership, empty ledger.)_
- [x] 8.6 Assert no pricing side effect: after a completed purchase, `CurrentPriceMicros` and the product's curve are unchanged. Verify a `DatabaseTest`. _(`ReservationAndPurchaseTests.A_completed_purchase_leaves_the_products_cached_price_untouched` — price unchanged, only `Scheduled` `PricePoints`.)_
- [x] 8.7 Price-drift-during-hold test: advance a `FakeTimeProvider` and `RecomputeAsync` while a reservation is active, then complete — the buyer pays exactly `QuotedPriceCents` and `BuyPriceCents` matches. Verify a `DatabaseTest`. _(`Price_movement_during_the_hold_does_not_change_what_the_buyer_pays` — cache moved, `purchase.PriceCents` and `BuyPriceCents` are still the original quote.)_
- [x] 8.8 Concurrency test: two parallel `ReserveAsync` calls for the same product — exactly one gets the reservation, the other is rejected and no charge occurs. Verify a `DatabaseTest`. _(Covered by 8.1's `A_second_concurrent_reserve_is_rejected_with_Reserved` — `Task.WhenAll` of two `ReserveAsync`, asserts 1 success / 1 `Reserved` / one active row. Reserve never touches payment, so "no charge" holds trivially.)_

## 9. Resale & buyback

- [x] 9.1 `Features/Marketplace/ResaleService.cs` — `ListAsync(ApplicationUser owner, Product, CancellationToken)`: reject non-owner, reject if already listed, reject if an active reservation exists; create an `Active` `ResaleListings` row. Verify `DatabaseTest`s for owner-lists, non-owner-denied, double-list-denied. _(Errors: `NotOwner` / `AlreadyListed` (incl. the `23505` race) / `ReservationInProgress`. Lazy-expires stale reservations first. `ResaleAndBuybackTests` — owner-lists (+ product becomes eligible for another buyer), non-owner, double-list, reservation-blocks.)_
- [x] 9.2 `ResaleService.DelistAsync(ApplicationUser owner, Product, CancellationToken)`: reject if an active reservation exists; set the active listing `Cancelled`. Verify a test asserts the product is no longer buyable by others. _(No-op when not listed; `NotOwner` when the active listing belongs to someone else. Test: after delist, `PurchaseEligibility` → `NotForSale` for another user.)_
- [x] 9.3 Peer purchase path: reserving against a listing carries `ResaleListingId`; completion closes it `Sold` and credits the seller. Verify a test: listing closed, seller credited `price - commission`, reservation consumed. _(`A_listed_product_can_be_reserved_and_bought_by_another_user` — full chain `ListAsync` → `ReserveAsync(…, listing)` (asserts `reservation.ResaleListingId == listing.Id`) → `CompleteAsync` → listing `Sold`, reservation `Consumed`, seller wallet == price, buyer is current owner. (Commission split itself is covered by §8.4's peer test.))_
- [x] 9.4 `BuybackService.BuybackAsync(ApplicationUser owner, Product, CancellationToken)` (no reservation): transaction, `SELECT … FOR UPDATE` on the product, re-run `BuybackEligibility`, close the owner ownership row, insert a marketplace-held row (`UserId = null`, type `BuybackReturn`), credit the owner the full `currentPriceCents` (`BuybackCredit`, no commission). Verify `DatabaseTest`s: successful buyback, eligibility re-check rejection, blocked while listed/reserved. _(Friendly `BuybackEligibility.CheckAsync` pre-check, then the same rules re-checked **inline under the product row lock** using the locked row's `CurrentPriceCents()` and `BuybackEligibility.WithinSpreadCap`. Owner wallet ensured before the tx. `ResaleAndBuybackTests`: success (marketplace-held `null` row + single `BuybackCredit` == current price), above-cap denied (no ledger, ownership intact), blocked-while-listed.)_
- [x] 9.5 Assert no pricing side effect after a buyback; assert the former owner is no longer the current owner and MAY re-buy once it is listed again. Verify a `DatabaseTest`. _(`After_a_buyback_the_former_owner_is_not_the_owner_and_may_buy_it_again_with_the_price_untouched` — `IsCurrentOwnerAsync` false, `PurchaseEligibility` → allowed (marketplace-held is directly buyable), `CurrentPriceMicros` unchanged, only `Scheduled` `PricePoints`.)_

## 10. Recurring jobs

- [x] 10.1 `Features/Jobs/PriceRecomputeJob.cs` — `RecomputeAllAsync(CancellationToken)`: page products, call `PricingEngine.RecomputeAsync` per product. Verify a `DatabaseTest` runs it and asserts `CurrentPriceMicros` and a `Scheduled` snapshot updated for each product. _(Loads product ids `AsNoTracking` and delegates per-product to `PricingEngine.RecomputeAsync` (its own tx + `FOR UPDATE` each). `MarketplaceJobsTests` — 3 products, each cached price == `PriceAt(now)` + one `Scheduled` snapshot.)_
- [x] 10.2 Register `PriceRecomputeJob.RecomputeAllAsync` every 10 minutes via `IRecurringJobManager.AddOrUpdate` (`options.PriceRecomputeCron`) in `AddMarketplace()`, with `[DisableConcurrentExecution]`. Verify a test asserts `AddOrUpdate` was called with the cron. _(`AddOrUpdate` can't run inside an `IServiceCollection` extension — moved to an `IHostedService` `MarketplaceRecurringJobs` registered by `AddMarketplace`; it runs on `app.Run()` only, not during `dotnet run -- seed`. `[DisableConcurrentExecution(300)]` on the method. Registers all three jobs (recompute `options.PriceRecomputeCron`, expiry `* * * * *`, reconcile `*/5 * * * *`). Test: `RecordingRecurringJobManager` captures the crons.)_
- [x] 10.3 Idempotency test: run `RecomputeAllAsync` twice at the same fixed instant → identical price; advance the `FakeTimeProvider` and run again → price follows the curve. Verify a `DatabaseTest`. _(`RecomputeAllAsync_is_stable_within_an_instant_and_follows_the_curve_over_time`.)_
- [x] 10.4 `Features/Jobs/ReservationExpiryJob.cs` — `ExpireStaleAsync(CancellationToken)`: set `Expired` on `Active` reservations past `ExpiresAt`. Register every minute. Verify a `DatabaseTest` seeds a stale reservation, runs it, and asserts the product is purchasable again and any peer listing is visible again. _(Delegates to the shared `ReservationExpiry.ExpireStaleAsync` (unscoped). Test: stale reservation → `Expired`, then `PurchaseEligibility` → allowed. Peer listing "visible again" is inherent — the listing is never closed while a hold is active, only excluded by the reservation check that now passes.)_
- [x] 10.5 Lazy expiry: `PurchaseEligibility` / purchasability checks expire a stale active reservation inline before deciding. Verify a test that an expired-but-not-swept reservation does not block a new buyer. _(Done in §7.2 — `ReservationExpiry.ExpireStaleAsync` runs first in `PurchaseEligibility.CheckAsync` / `BuybackEligibility.CheckAsync` / `ResaleService`. `PurchaseEligibilityTests.A_stale_reservation_…_is_expired_inline_and_does_not_block`.)_
- [x] 10.6 `Features/Jobs/MarketplaceReconcileJob.cs` — `SweepAsync(CancellationToken)` sweeping `Paid` purchases with no `Completed` transition older than N minutes and logging/auto-reversing. Verify a `DatabaseTest` seeds a stuck purchase and asserts it is caught. _(`StuckAfter = 15 min` (`static readonly` on the job — a knob that could move to config later). Reverses each via `RefundService.ReverseAsync`, per-purchase try/catch so one failure doesn't stop the sweep. `MarketplaceJobsTests`: stuck `Paid` (1h old) → refunded + `Reversed` + no ledger; recent `Paid` (1min) → untouched.)_

## 11. Refunds & reversals

- [x] 11.1 `Features/Marketplace/RefundService.cs` — `ReverseAsync(Purchase, CancellationToken)`: refund the buyer's Stripe payment, claw back the seller credit with a compensating `RefundClawback` entry (balance may go negative → `Cashable = false`), return ownership to the seller if product state allows, set the purchase `Reversed`. Verify a `DatabaseTest` asserts a balanced ledger, the `Cashable` flag, and restored ownership. _(Branches on status: a stuck `Paid` purchase (never transferred) is only refunded + `Reversed`; a `Completed` one is fully unwound — refund, one `RefundClawback` of `-(PriceCents − CommissionCents)` (the net the seller received), and ownership returned to the seller **only if the buyer still holds it** (a downstream sale can't be unwound — logged). Prior buy price restored from the seller's most-recent released holding. `RefundServiceTests`: 4 cases incl. clawback-drives-negative → not cashable, and non-Paid/Completed → `InvalidOperationException`.)_

## 12. Catalog & listing UI

- [x] 12.1 `Features/Marketplace/CatalogQuery.cs` — pagination (`options.CatalogPageSize`), filters (ownership state, resale state, text search on title/description), ordering with the resale boost (active-listing products first within the sort grouping), exclusion of products reserved by others. Verify xUnit `DatabaseTest`s for each filter and for boost ordering (listed A before unowned B). _(`ICatalogQuery` seam. `BrowseAsync(CatalogFilter, viewerId, ct)` → `CatalogPage` of `CatalogItem` records (slug / title / primary-image id / price cents / growth cents / `IsCollectorListed` / `ViewerHasReservation`). Buyable-only scope (marketplace-held OR collector-listed, never the viewer's own, never another user's active reservation); lazy-expires stale reservations first; `ILIKE` text search; `MinPriceCents`/`MaxPriceCents`; `OrderByDescending(listed) ThenBy(sort)` for the boost; growth via `PricingEngine.GrowthLast12Months(seed, createdAt, now)` value overload (no `Product` construction → arch test clean). `CatalogQueryTests` — 8 cases.)_
- [x] 12.2 `Components/Pages/Marketplace/Catalog.razor` (+ `.razor.cs`) — MudBlazor card/grid entries with title, primary thumbnail (`GET /api/files/{id}`), current price (USD), "Growth last year" amount, viewer-specific purchasable state, "owned asset" badge on listed-owned entries, "owned by you" marker, "reserved" marker; `MudPagination`. Verify a bUnit test asserts badge/marker visibility and that owned entries show no buy action for the owner. _(`@page "/marketplace"`, public. `MudGrid`/`MudCard` grid, `MudTextField` (debounced) + min/max `MudNumericField` + sort `MudSelect` + "Clear filters" (only when active), `MudPagination`. `MarketplaceFormat.Usd`/`SignedUsd` helper. Owned entries can't appear (they're filtered out in `CatalogQuery`), so there is no owner buy action to hide. `CatalogPageTests` — 2 bUnit tests (badge/marker placement + detail links; empty state).)_
- [x] 12.3 `Components/Pages/Marketplace/ProductDetail.razor` (+ `.razor.cs`) — MudBlazor image gallery, the price-history chart (tasks 12.4–12.6), creation date, "Growth last year", the "Certificate ID" row (task 4.4), contextual single action derived server-side from the eligibility services, no download control. Verify a bUnit test per viewer role. _(`@page "/marketplace/{Slug}"`, public. Backed by an `IProductView` seam → `ProductViewModel` (product + gallery + ownership/listing/reservation state + price/growth + the one `ViewerAction` + `CanSellBack`), and an `IMarketplaceActions` facade over reserve/list/delist/buyback. `MudCarousel` gallery, `MudSimpleTable` metadata, cert-ID row via `CertificateId.For(...)` + "Registered to you" for the owner. `ProductDetailPageTests` — 5 bUnit cases (anon/owner/buyer/ranges/not-found). This folds in **4.4** (cert-ID render surfaces on detail).)_
- [x] 12.4 Chart range selection on `ProductDetail`: `[SupplyParameterFromQuery] string? ChartRange`; `OnParametersSet` resolves it to one of `24h|7d|30d|1y|all`, defaulting to `all` when the product is younger than 365 days else `1y`, falling back to that default for any unrecognised value; a `MudToggleGroup` of range buttons calls `NavigationManager.NavigateTo("?chartRange=…", replace: true)`. Verify bUnit tests: a 2-month-old product defaults to `all`; a 3-year-old defaults to `1y`; `?chartRange=bogus` falls back; clicking `24h` updates the query string and selected state. _(`PriceChartSampler.DefaultRange` / `NormalizeRange` (pure, unit-tested), `MudToggleGroup` → `Nav.GetUriWithQueryParameter("chartRange", …)`. `PriceChartSamplerTests` covers default/fallback; `ProductDetailPageTests` covers the toggle rendering.)_
- [x] 12.5 `History()` on `ProductDetail`: returns `IReadOnlyList<(DateTimeOffset T, decimal Price)>` per the design §13 table (window + step by range, window start clamped to `product.CreatedAt`, `Price = Math.Round(PricingEngine.PriceAt(product, cursor) / 1_000_000m, 2)`, a final point at `timeProvider.GetUtcNow()` appended). Verify xUnit/bUnit tests: switching `1y` → `24h` changes point count and spacing; no point's `T` precedes `CreatedAt`; the last point is "now"; the `all` range yields ≤ 181 points for a 2015 product. _(Extracted to a pure `Components/Pages/Marketplace/PriceChartSampler.Sample(seed, createdAt, range, now)` (uses the value-based `PricingEngine.PriceAt` overload). `PriceChartSamplerTests` — 6 cases incl. all-range ≤182 points, first point == $1.00 when the window starts at creation.)_
- [x] 12.6 Chart rendering + hover: `MudChart` `ChartType.Line` (confirm the API against the pinned MudBlazor version) or a hand-authored inline SVG partial `Components/Pages/Marketplace/PriceChart.razor` — line + faint area fill + gridlines + endpoint dot in an `overflow-x: auto` container; `@onpointermove` maps `OffsetX` → nearest sample → a vertical guide + dot + floating `date · $price` label (flips left within ~80px of the right edge), `@onpointerleave` hides them; a `first → last` visible-window caption and left/right axis labels that update with `ChartRange`. Verify bUnit tests: a smoke render of each of the five ranges; a simulated pointer move asserts the label matches the nearest sample; the caption changes between two ranges. _(**Hand-authored inline SVG** `PriceChart.razor` (+ `.razor.css`, theme-token colours) — fixed 640px viewBox in an `overflow-x:auto` container so `OffsetX` maps straight to the sample index without JS; `@onpointermove`/`@onpointerleave` guide + marker + `date · $price` readout (flips past 75%), `first → last` caption, per-range axis-label formats. Server-side pointer events (Interactive Server) — a round-trip per move, acceptable for this chart. Smoke-covered by `ProductDetailPageTests`; a dedicated pointer-move bUnit test is a good follow-up.)_

## 13. Wallet & assets UI

- [ ] 13.1 `Components/Pages/Marketplace/Wallet.razor` (`[Authorize]`) — balance and a paginated read-only `MudDataGrid` ledger, with "store credit — cash-out coming later" copy; a negative balance shows as owed / not cashable; no spend/withdraw controls. Verify a bUnit test asserts a `SaleCredit` entry appears after a peer sale.
- [ ] 13.2 `Components/Pages/Marketplace/MyAssets.razor` (`[Authorize]`) — owned products with buy price, current price, delta, trailing-12-month growth, resale state, and buyback availability from `BuybackEligibility`. Verify a bUnit test asserts the buyback button is hidden when above the spread cap.
- [ ] 13.3 Buy flow on `ProductDetail.razor`: `ReservationService.ReserveAsync` (service via DI) → a `MudDialog` showing the locked quoted price and a countdown to `ExpiresAt` → `wwwroot/js/stripe-checkout.js` interop confirms the full-price PaymentIntent → `PurchaseService.CompleteAsync` server-side; show "reserved, try again later" on a failed reservation and "payment failed" on gateway failure. Verify bUnit tests for a successful purchase and a gateway-failure purchase (fake gateway + fake interop).

## 14. Routes, policies, wiring

- [ ] 14.1 `@page` routes for `/marketplace`, `/marketplace/{Slug}`, `/marketplace/wallet` (`[Authorize]`), `/marketplace/assets` (`[Authorize]`); a `NavMenu` entry. Verify a bUnit/integration test: an anonymous hit on `/marketplace/wallet` renders `RedirectToLogin`.
- [ ] 14.2 Authorization: `ReservationService`/`ResaleService`/`BuybackService` re-check current ownership in code; reserve/complete deny the current owner. Verify xUnit tests deny the wrong user at the service layer.
- [ ] 14.3 Register `MapStripeWebhookEndpoints()` in `Program.cs`; add `Endpoints/Marketplace.http` with the public catalog GETs runnable and the webhook/buy POSTs documented as needing a real session / the Stripe CLI. Verify the endpoint responds 400 to an unsigned body.

## 15. Full-flow verification

- [ ] 15.1 End-to-end xUnit `DatabaseTest`: seed a product → user A reserves and buys from the marketplace with a full Stripe charge (fake gateway) at the quoted price → run `PriceRecomputeJob.RecomputeAllAsync`, price moved on its curve, unaffected by the sale → A lists for resale → user B reserves and buys → A's wallet credited `price - commission` (store credit, not spendable), listing closed → B sells back to the marketplace only if within the $10 cap (assert blocked once above it) → assert A can re-buy once it is listed again. Run `dotnet test --filter FullyQualifiedName~Marketplace`.
- [ ] 15.2 Run `dotnet format DigitalHouse.slnx --verify-no-changes` and the full `dotnet test` suite; verify green.
- [ ] 15.3 Run `openspec validate add-digital-asset-marketplace --strict` and confirm the change passes.
